<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!--
      録画データは同じ署名（同じ鍵）のアプリ（BGViewer）だけが読めるようにする。
      protectionLevel="signature" なので、別の鍵で署名された他のアプリはこの権限を
      絶対に取得できず、Providerへのアクセスは自動的に拒否される。
    -->
    <permission
        android:name="com.hono.bgrecorder.permission.ACCESS_RECORDINGS"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BGRecorder"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.BGRecorder">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".RecordingService"
            android:exported="false"
            android:foregroundServiceType="camera|microphone" />

        <!--
          録画一覧・動画データを公開するProvider。
          読み書きとも signature 権限必須 = 同じ鍵で署名されたBGViewerだけがアクセスできる。
          Gallery・ファイル管理アプリ・他のあらゆるアプリは、この権限を取得できないため
          クエリも再生もファイル読み出しも一切できない。
        -->
        <provider
            android:name=".RecordingsProvider"
            android:authorities="com.hono.bgrecorder.provider"
            android:exported="true"
            android:grantUriPermissions="false"
            android:readPermission="com.hono.bgrecorder.permission.ACCESS_RECORDINGS"
            android:writePermission="com.hono.bgrecorder.permission.ACCESS_RECORDINGS" />
    </application>
</manifest>
